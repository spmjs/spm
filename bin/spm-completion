#!/usr/bin/env node


// Installation: spm completion >> ~/.bashrc  (or ~/.zshrc)

var fs = require('fs');
var join = require('path').join;

var content = fs.readFileSync(join(__dirname, '../scripts/.spm_completion'), 'utf-8');
console.log(content);
