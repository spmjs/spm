
// 模板替换

/**
 * 读取模板文件，内嵌到项目文件中。减少请求。
 *
 * @param {Object} project 项目模型.
 */
exports.execute = function(project) {

};


//var template = require('./calendar.tpl');
//TODO 是否在模块替换里面进行替换，应该没有必要单独在起一个plugin了。
