{
  "version": "0.0.1",
  "name": "spmConfigModel",
  "spmConfig": {
     "*": {
       "only": true,
       "src": "app",
       "idRule": "{{moduleName}}"
     },
     "build": {
       "abc": "abc/{{version}}",
       "to": "{{spmConfig.build.abc}}/def/{{spmConfig.build.src}}"
      }
  }
}

